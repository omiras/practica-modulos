/**
 * Endpoint es recibirun  y nos va a deolver un objeto, con dos propiedades. la primera propeidad será decirnos si el número es par o impar, y lo segunda será ese número elevado al cuadrado
 */
import { isOdd, pow2 } from "./utils/tools.js";
console.log(isOdd(3));
console.log(pow2(3));
